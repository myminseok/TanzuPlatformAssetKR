#@ load("@ytt:overlay", "overlay")
#@overlay/match by=overlay.all, expects="0+"
---
metadata:
  #@overlay/match missing_ok=True
  #@overlay/remove
  ownerReferences:
#@overlay/match missing_ok=True  
#@overlay/remove
status:
